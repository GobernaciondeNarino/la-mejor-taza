// Plantilla de configuración Firebase.
// 1. Copia este archivo a `firebase-config.js` (ya está en .gitignore).
// 2. Pega los valores que te entrega la consola de Firebase
//    (Project settings → General → Your apps → SDK setup and configuration).
// 3. Recarga la página: el front detecta el archivo y conmuta a Firestore en vivo.

export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "la-mejor-taza.firebaseapp.com",
  projectId: "la-mejor-taza",
  storageBucket: "la-mejor-taza.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
